/** Tab ids for dashboard URL (`?dash=`) and client tablist — kept in a tiny module for server actions. */
export type DashboardMainTabId =
  | "setup"
  | "shopProfile"
  | "itemGuidelines"
  | "requestListing"
  | "bugFeedback"
  | "listings"
  | "promotions"
  | "notifications"
  | "support"
  | "orders";
