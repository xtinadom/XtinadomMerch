/** Max featured carousel slots on `/shop/all` (admin picks + 30-day sales fill). */
export const PLATFORM_ALL_PAGE_FEATURED_LIMIT = 10;

/** Max items in the marketing home “Hot items” carousel (admin picks + view-based fill). */
export const HOME_HOT_CAROUSEL_LIMIT = PLATFORM_ALL_PAGE_FEATURED_LIMIT;
